# 腾讯云 COS 配置指引（美甲师计分系统）

> 本文档教你从零开通腾讯云对象存储（COS），创建存储桶，并生成一把**只对该桶有权限、最小风险**的密钥，供「美甲师计分系统」前端使用。
> 全程约 15 分钟，无需付费（按量计费，个人小数据几乎为 0 元）。

---

## 一、准备一个腾讯云账号

1. 打开 https://cloud.tencent.com ，点击右上角「**注册**」。
2. 用微信/QQ/手机号任选一种方式注册，按提示完成实名认证（个人实名即可）。
3. 登录后进入控制台。

---

## 二、开通对象存储 COS 服务

1. 在控制台顶部搜索框输入「**对象存储**」，或直接访问
   https://console.cloud.tencent.com/cos
2. 首次进入会提示「**开通 COS 服务**」，点击确认，按提示完成开通。
3. 开通免费，之后**按实际用量计费**，只存一个几 KB~几 MB 的 JSON 文件，每月费用基本为 0。

---

## 三、创建存储桶（Bucket）

1. 在 COS 控制台「存储桶列表」页，点击「**创建存储桶**」。
2. 填写配置：

   | 配置项 | 填写内容 |
   |--------|----------|
   | **名称** | 建议 `nail-score-data`（只能小写字母、数字和 `-`） |
   | **所属地域** | 选离你近的，比如「**广州 ap-guangzhou**」或「上海 ap-shanghai」 |
   | **访问权限** | **选「私有读写」**（Public Read 不要开，安全） |
   | 其他选项 | 保持默认即可 |

3. 点击「**创建**」。

> ⚠️ 记下两样东西，后面要用：
> - **桶名称**：`nail-score-data-<你的APPID>`（创建后控制台会显示完整名称，含一串数字后缀）
> - **地域**：如 `ap-guangzhou`

---

## 四、创建「最小权限」子密钥（关键步骤，务必照做）

> 不要直接用你账号的主密钥！我们要新建一把**只允许访问这一个桶**的子密钥，把泄露风险压到最低。

### 4.1 进入访问管理

- 访问 https://console.cloud.tencent.com/cam/capi （API 密钥管理）
- 或控制台搜索「**访问管理 CAM**」→「**访问密钥**」→「**API 密钥管理**」

### 4.2 新建子用户（推荐，最安全）

1. 左侧菜单「**用户**」→「**用户列表**」→「**新建用户**」。
2. 选择「**自定义创建**」。
3. 访问方式：勾选「**编程访问**」（只需 API 密钥，不要勾选控制台访问）。
4. 权限设置：选「**从策略列表中直接选取**」，然后在搜索框搜 `QcloudCOSFullAccess`，勾选它。
5. 创建完成后，**立即下载或记下**该子用户的：
   - **SecretId**
   - **SecretKey**
   - 保存一次即可，之后不再显示。

> 如果不想建子用户，也可以直接用主账号的 API 密钥（步骤 4.1 页面的「新建密钥」）。但**强烈建议用子用户**，主密钥泄露会影响你腾讯云账号下所有资源。

### 4.3 （可选但推荐）把子密钥权限收窄到只访问这一个桶

如果你想让密钥更安全，可以在上面第 4 步的基础上，改用「自定义策略」，只允许操作 `nail-score-data` 桶：

1. 访问管理 →「**策略**」→「**新建自定义策略**」→「按策略语法创建」。
2. 粘贴下面这段 JSON（把 `你的APPID` 换成你的真实 APPID）：

```json
{
  "version": "2.0",
  "statement": [
    {
      "effect": "allow",
      "action": [
        "cos:PutObject",
        "cos:GetObject",
        "cos:HeadObject",
        "cos:GetBucket"
      ],
      "resource": [
        "qcs::cos:ap-guangzhou:uid/你的APPID:nail-score-data-你的APPID/*",
        "qcs::cos:ap-guangzhou:uid/你的APPID:nail-score-data-你的APPID"
      ]
    }
  ]
}
```

> ⚠️ 如果你把桶建在**其他地域**，把 `ap-guangzhou` 换成对应地域代码。
> 3. 保存策略，然后在给子用户授权时选这把「自定义策略」而不是 `QcloudCOSFullAccess`。

---

## 五、把密钥填入「美甲师计分系统」

拿到 4 样信息后，在应用的「**同步设置**」页面填入：

| 字段 | 填什么 |
|------|--------|
| **COS 桶名称** | 如 `nail-score-data-1250000000`（含 APPID 后缀的完整名称） |
| **COS 地域** | 如 `ap-guangzhou` |
| **COS SecretId** | 你刚创建的 SecretId |
| **COS SecretKey** | 你刚创建的 SecretKey |

填好后点「**保存 COS 配置**」，再点「**手动同步**」，就会把数据推送到腾讯云 COS。

---

## 六、常见问题

- **为什么还要填 GitHub Token？** —— 你选了「双写/保留 GitHub」，所以两个都填，推送给双份，任何一边坏了另一边还能用。
- **推送失败 / 提示网络错误？** —— COS 在国内不被墙，如果还失败，多半是桶名称、地域或密钥填错了，检查是否有空格、APPID 后缀是否完整。
- **想验证 COS 通不通？** —— 保存配置后点「测试 COS 连接」，会有明确成功/失败提示。
- **担心费用？** —— 你的数据是单个 JSON 文件（几 KB），COS 免费额度足够，每月 0 元。

---

✅ 配置完成后，你的手机、电脑都能通过腾讯云 COS 正常同步了，不再受 GitHub 被墙影响。
